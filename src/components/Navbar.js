import React from 'react';

const navbar = (props) => (
  <nav className="h3 w-100 border-box bg-wg-red v-mid font-nunito flex justify-between ph3">
    <a href="#" className="link f4 mt1 pt3 pb3 wg-broken-white">WartegApp</a>
    <a href="#"  className="link f4 mt1 pt3 pb3 wg-broken-white">Log In</a>
  </nav>
);

export default navbar;